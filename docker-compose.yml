services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./database/migrations:/docker-entrypoint-initdb.d
    networks:
      - internal

  fastapi:
    build: ./backend
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
    depends_on:
      - postgres
    networks:
      - internal

  next:
    build: ./frontend
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      FASTAPI_URL: ${FASTAPI_URL:-http://fastapi:8000}
    depends_on:
      - postgres
      - fastapi
    networks:
      - internal
      - external

  nginx:
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      # Scope cert mounts to this domain only. Both live/ and archive/ are
      # required: live/ holds relative symlinks that resolve into archive/.
      - /etc/letsencrypt/live/hebrai.co:/etc/letsencrypt/live/hebrai.co:ro
      - /etc/letsencrypt/archive/hebrai.co:/etc/letsencrypt/archive/hebrai.co:ro
    depends_on:
      - next
    networks:
      - external

networks:
  internal:
    internal: true
  external:

volumes:
  postgres_data:
