@../../AGENTS.md

# Frontend (Next.js) — Local Instructions

When working inside `frontend/`, also read the root AGENTS.md operational contract.

Always consult `node_modules/next/dist/docs/` before writing framework-specific code — Next.js 16 has breaking API changes from earlier versions.
